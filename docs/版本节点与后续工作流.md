# AxisMeasurement 版本节点与后续工作流

## 协作与验证约定（2026-09-15起）

- 用户要求恢复此前的软件升级协作方式：Codex负责读取现状、分析反馈并修改源码、工程文件和文档；不主动执行编译、测试、启动程序或生成测试截图、测试程序和构建日志。
- 每轮改动完成后明确列出修改内容、涉及文件、建议验证步骤和当前“未编译/未测试”状态。用户在本机完成编译及实图/实机测试，再返回构建输出、完整诊断、截图或现象；Codex据此继续修改并更新验证结论。
- 只有用户随后明确要求Codex编译或测试时，才临时执行相应操作。既有`x64`构建与测试产物保持原状，不因本约定自动删除。

## 节点 AM-GUI-20260916-03：工程、设备点位与基础测量配置

- 记录日期：2026-09-16。
- 工作仓库：`E:\331Software_Backup\_github_upload`，分支`main`。
- 本节点基线HEAD：`7d9c190e5967d660c1abe40a7fcc4d54b6311eeb`（`图形化检测参数与单角度测量`）。
- 上传提交：`9cbb19699b7a66a136fd3640b76a19acf0c082d6`（`图形化工程与基础测量配置升级`）。
- 远端状态：2026-09-16核对本地`HEAD`与`origin/main`均指向上述提交，节点已在远端确认。节点名称是文档标识，不等同于已创建Git tag。
- 节点范围：工程JSON保存恢复、相机采集入口和分类设备点位、图形化窗口非模态操作、直径光幕缓存采样、孔径均布个数配置，以及相关失效与离线防伪值校验。

### 用户验证证据

| 项目 | 结果 | 结论边界 |
|---|---|---|
| `Release|x64` | 用户多轮反馈构建成功；孔径配置改动后再次成功 | 当前源码编译链接通过 |
| 工程保存恢复 | 图像、图形、记录、点位配置可恢复；中央空态文字已消失 | 主保存恢复流程通过；损坏JSON等增强用例仍保留 |
| 窗口操作 | 主窗口切换、图形化窗口拖动、最小化均正常 | 离线窗口交互通过；硬件停止联动待现场验证 |
| 直径光幕离线状态 | 状态区正常，无硬件时未产生伪值，旧主窗口正常 | 离线闭环通过；真实OUT1、补偿精度和断线恢复待硬件验证 |
| 孔径均布个数 | 类型启用状态正确；未设置时明确拦截；数值6保存恢复正确；改为长度后仍可保存恢复 | 孔径配置基础闭环通过；孔径图像算法尚未接入 |

孔径未设置时的用户截图提示原文为：“孔径记录需要填写孔均布个数。该值对应旧表单的‘均布个数’，不是H0/H1拍照位置。”

### 上传边界与后续入口

- 本节点上传前不再加入新功能。用户完成提交与推送后，补记提交SHA并核对远端；节点名称只是文档标识，不代表已创建Git tag。
- 单ROI小倒角重复稳定性、真实相机采集、真实轴点位、真实光幕和全部硬件停止联动没有被本节点离线结果覆盖。
- 下一开发入口为孔径测量逐型接入：先确定目标产品、H0/H1拍照位置、对应旧算法实现和样图，再抽取可配置测量区域及失败诊断；不得直接选择一个硬编码程序函数作为通用孔径算法。

节点上传后已开始孔径首个算法候选：孔径记录关联一个圆形或矩形面积ROI，由ROI自动生成孔上、下方的局部测量位置；随后沿用原软件的图像增强、HALCON line metrology、Tukey拟合、四组跨边端点距离最大值和`CalikKong`换算。加入标定保存、相机来源校验、空结果保护和阶段错误；按协作约定尚未由Codex编译或执行样图。

## 后续进度（2026-09-14）

- 2026-09-15用户实图试测单ROI时报告“HALCON 2: Illegal operation on tuple”，阶段为“单ROI直线拟合”。源码核查发现`cont_approx`全局属性被通过整数接口`HTuple::I()`读取；先修正读取并补充元组保护，随后在下一张实图上已越过该错误。之后算法改为直接按直线段分割，不再读取该属性。
- 同一轮实图进一步显示“长度筛选后2条轮廓→有效直线1条→边对0”：边缘和长度筛选实际成功，首版新增的曲线类型、1.5 px偏差及5 px角点间隙约束过严。已按用户原流程改为`lines`分割后逐段拟合，默认偏差调整为2.5 px、角点间隙20 px，并增加逐类排除计数。改动需用该图复测，不能仅凭截图保证已识别。
- 上述调整后Release x64构建成功（`x64/single_roi_lines_build.log`），离线几何、记录参数/模式及轴控制回归退出码0。由于只有界面截图而无原始图像，本地未执行该图的HALCON识别；下一证据为用户在新版程序中用同一原图和ROI复测后的完整诊断。
- 用户复测反馈“经常失败，但是成功一次”。成功图得到58.9969°、2条有效直线和唯一边对；失败图明确为2条长度合格直线段中1条被偏差条件排除。说明角度计算和配对链可达，但逐点最大偏差对ROI位置、圆角和模糊过渡过敏。质量门槛改为点线偏差P90：允许约10%异常点，偏离比例较高的曲线仍拒绝；该稳定性改动待同图多次复测。
- P90源码编译通过；因用户试测中的标准exe仍在运行，标准链接遇到LNK1104文件锁，随后成功生成`x64/Release/AxisMeasurement_p90.exe`（构建日志`x64/corner_p90_build.log`）。离线回归退出码0，新增覆盖单异常点容忍、异常点比例过高和非有限偏差拒绝。标准exe尚未被本轮覆盖。
- 用户进一步要求应用项目审查Skill，并确认新算法是否直接调用原软件。源码复核结论：原`Straight_TLineFP_P`通过已知引导线建立HALCON Metrology对象，取得一条结果轮廓后以Tukey参数(-1,0,5,2)拟合；原程序倒角角度多由单条拟合线相对坐标轴计算。当前“一个ROI自动识别两条线并求夹角”没有可直接调用的旧完整函数，只复用了相同拟合算子与参数；此前将两者描述为相同流程会高估复用程度。
- 为降低单ROI对框选位置的敏感性，新增`RegressContoursXld`与`UnionCollinearContoursXld`，在长度筛选前合并同一物理边被模糊、毛刺或圆角端部切碎的近似共线段；方向差限0.10 rad，避免把两条夹角边合并。角点位于ROI边界内外2 px均接纳，仍受图像范围和两侧端点间隙限制。P90偏差改动一并保留。本轮按用户约定只修改，未编译、未测试，等待用户返回结果。
- Release|x64曾出现Visual Studio Design-time生成失败。`%TEMP%`日志确认并非C++或HALCON错误：一个并行Qt任务把`AxisMeasurement.ui`错误解析为Qt安装目录下的相对路径，另一并行任务使用仓库路径并成功生成同一头文件。工程中的主UI项已改用`$(ProjectDir)AxisMeasurement.ui`，用户随后回复“可以继续”；该回复只记录为工作流解除阻塞，不扩大为完整Release构建验收。
- 单ROI新增阶段诊断保护：直线分段、共线合并、长度筛选的空结果直接停止并保留累计数量；HALCON异常报告实际阶段，不再把后段错误统一误报为“直线分段”。同时修正诊断文字中的重复百分号。本轮仍按约定未编译、未运行实图。

- 当前已核对本地HEAD为d90b997“修复 sdk_assist 合并冲突导致的编译错误”，工作区在检测参数开发前干净。该提交已修正c412de1中的冲突标记并合并布局与图形化入口检查；没有联网确认远端最新提交。
- 工作流第1项“检测参数与失败诊断”已接入：每记录六项参数、专用应用与失效、阶段轮廓计数、许可错误分类。Release x64及扩展后的离线回归通过，实际HALCON试测待验收，未新增Git提交或tag。
- 用户随后回复“确认”，继续第2项单ROI倒角；该回复不替代实图算法测试日志。单ROI首版已接入模式、相邻直线边对筛选、唯一自动输出/多候选选择、角区叠加及失效处理。新头文件graphical_corner_geometry.h已登记工程，尚未提交或推送。
- 验证：Release x64构建成功（x64/single_roi_angle_build.log）；tests/run_graphical_axis_panel_test.ps1退出码0。覆盖45°/90°、端点反向、间隙阈值、平行/退化/非有限值拒绝、角区路径，以及单/双ROI记录切换、未提交模式、ROI2入口限制、检测参数与原轴控制回归。截图x64/single_roi_angle.png已检查；大字体主题下侧栏仍有横向滚动，作为布局旁支保留。未执行真实HALCON提取、多候选实图选择或硬件运动。
- 本阶段实际HALCON图像回归仍待匹配18.11运行库和样图验证。原计划在小倒角稳定性闭环后推进工程保存；用户随后明确要求继续开发，现已接入工程保存候选版，同时保持单ROI稳定性为独立待验证项。其余顺序和现场硬件验收边界保持如下规划。

## 节点 AM-GUI-20260915-02：检测参数与单ROI倒角候选版

- 记录日期：2026-09-15。
- 工作仓库：`E:\331Software_Backup\_github_upload`，分支`main`。
- 本节点基线HEAD：`d90b9976ec1092ca61511a62dbd9d5a0239efcdc`（“修复 sdk_assist 合并冲突导致的编译错误”）。
- 上传提交：`7d9c190e5967d660c1abe40a7fcc4d54b6311eeb`（`图形化检测参数与单角度测量`）。
- 远端状态：2026-09-15只读核对本地`HEAD`与`origin/main`均指向上述提交，节点已在远端确认。节点名称是文档标识，不等同于已创建Git tag。

### 本节点目标与范围

1. 为圆弧、双ROI直线和单ROI倒角记录检测参数，校验参数范围，并在参数、ROI或模式变化后清除旧结果。
2. 角度测量保留双ROI模式，新增一个ROI框选相邻角/倒角的入口；从HALCON直线分段结果拟合两条相邻边，计算较小夹角或补角，并显示候选边对与只读叠加。
3. 单ROI采用`lines`分段、近共线碎片合并、Tukey直线拟合和P90点线偏差门槛；角点须接近两条拟合边端点并位于图像及ROI边界容差内。
4. 诊断区记录边缘/分段/合并/长度筛选/拟合/配对阶段及排除数量；空结果和HALCON异常停止输出，不沿用旧角度。
5. `AxisMeasurement.ui`的Qt UIC工程项使用`$(ProjectDir)`显式路径，避免Visual Studio Design-time任务把主UI相对路径解析到Qt安装目录。

### 本节点文件清单

- 工程注册：`AxisMeasurement.vcxproj`、`AxisMeasurement.vcxproj.filters`。
- 算法与界面：`myCode/head/graphical_program_editor.h`、`myCode/head/graphical_corner_geometry.h`、`myCode/sourceCode/graphical_program_editor.cpp`。
- 离线用例：`tests/graphical_axis_panel_test.cpp`。
- 文档：`docs/图形化二次开发模块需求与架构.md`、`docs/版本节点与后续工作流.md`、`docs/面板功能测试指南.md`。

### 已有证据与当前边界

| 项目 | 证据 | 节点结论 |
|---|---|---|
| 早期单ROI闭环 | 早期版本Release x64构建与离线回归通过 | 证明记录、候选与几何链曾可编译运行，不覆盖此后新增的共线合并及阶段保护 |
| 实图可达性 | 用户截图中曾成功输出58.9969°、2条有效直线和唯一边对 | 证明一组图像/ROI可到达计算结果，不证明重复稳定性或尺寸精度 |
| 实图失败定位 | 失败截图显示2条长度合格线中1条被偏差门槛排除 | 已据此把最大偏差改为P90，并增加近共线碎片合并；最终效果待复测 |
| 最新源码 | XML结构与`git diff --check`只读检查未发现结构/空白错误 | 最新共线合并、阶段保护和UIC路径改动按协作约定未编译、未测试 |
| Design-time故障 | `%TEMP%`日志显示错误路径来自Qt并行设计期任务；另一任务用正确路径生成成功 | 已改显式项目路径；用户回复可以继续，仅说明工作流解除阻塞 |
| 真实硬件与精度 | 本节点没有新增硬件运动、相机标定或标准件重复性证据 | 不声明轴运动安全、毫米精度或现场验收通过 |

### 上传、回退与补记

- 本次提交应包含上述9个路径，尤其不能遗漏未跟踪的`myCode/head/graphical_corner_geometry.h`；该头文件已登记到工程和筛选器。
- `x64/`中的EXE、OBJ、截图和构建日志被Git忽略，不会随源码上传；它们也不作为远端源码节点的一部分。
- 上传后记录准确提交SHA，并用`git status`确认工作区是否干净、用远端页面或`git log origin/main`确认远端包含该SHA，再把本节点标为“远端已确认”。
- 若最新源码不能编译或同图稳定性明显退化，以本节点基线`d90b997`作为源码回退点；不要用旧EXE覆盖新的配置或测量数据来代替源码回退。

## 本节点后的工作流

### A. 先关闭单ROI稳定性验证闭环

1. 用户编译当前提交的`Release|x64`；记录完整编译结论。若失败，先修复编译，不同时调整识别参数。
2. 固定同一张原图、同一ROI和同一组已应用参数，连续试测至少10次。固定输入若时成时败，按程序状态或未初始化数据问题处理；每次一致后再测试ROI位置敏感性。
3. 将ROI分别向四个方向小幅移动，并覆盖普通倒角、小倒角、低对比度、圆角过渡、第三条干扰边和仅一条边。保存完整“分段→共线合并→长度筛选→有效直线→相邻边对”诊断及叠加截图。
4. 按失败阶段修改：长度筛选为0时核对最短长度；P90排除时复核直线内点比例；有效直线不少于2但边对为0时复核角点间隙和端点；多边对时使用候选选择，不静默选择最长两条。
5. 通过条件：固定输入结果一致；合理小幅移动ROI仍能找到同一物理边对；错误候选可识别并选择；双ROI角度和圆弧没有回归。角度精度须另用标注样图或标准件验证。

### B. 稳定后冻结检测节点

- 将用户的构建结果、十次重复值、关键诊断和失败样例补入本节点；只有证据满足上述条件后，才把“候选版”提升为稳定基线。
- 不在缺少实图证据时继续叠加自动降阈值或自动放宽门槛，避免把噪声边变成看似成功的角度。
- 保留双ROI作为明确可控的备用流程；单ROI未通过的图像可回退到分别框选两条直线，而不是输出未经确认的结果。

### C. 已完成主流程：工程保存与恢复

- 版本1 JSON工程保存原图相对路径、尺寸和SHA-256，点/线/矩形/圆/三点圆弧的原图坐标与ID、测量记录顺序/关联/公差、检测参数、角度模式和候选选择审计信息。点位结构继续兼容原有`uncollected/none/pulse`记录。
- 保存使用`QSaveFile`提交，`Ctrl+S`在已有工程路径上保存；首次保存选择`.axisproj.json`。读取先完整解析并验证格式版本、字段类型、整数ID、图形边界、引用关系、参数、图像尺寸及内容哈希，再替换当前画布。缺图、同尺寸图像被替换、损坏JSON或无效引用均应拒绝且保留当前工程。
- 载入不恢复旧测量值或旧候选编号，所有记录标为需重新试测和重新确认候选。单ROI算法本轮未继续调整，稳定性仍等待用户按A节返回实图结果。
- 用户于2026-09-15反馈工程功能均通过；截图确认工程重新载入后原图、4个图形、圆弧半径/单ROI角度/双ROI角度共3条记录及其关联恢复，且历史结果均显示需重新试测。图像和图形可正常移动。
- 同次截图发现画布空态文字“尚未打开图像”在工程载入后仍覆盖图像。原因是本地图像入口会隐藏`canvasEmptyHint`，工程载入入口遗漏同一处理；源码已补齐，按协作约定未代替用户编译，需在下一次构建后复查文字消失。
- 损坏JSON、缺图、同尺寸图像替换等防护用例没有随截图提供逐项证据，继续保留为增强验证边界，不影响本次主保存—恢复流程结论。
- 通过条件：保存—关闭—打开后图形、关联、参数和顺序一致；路径、图像变更或引用错误可诊断且不覆盖当前工作；旧结果不会被误当成当前有效结果。

### D. 当前开发项：相机采集与设备点位关联

- 已接入相机0远心、相机1测孔、相机2粗糙度的连续采集、停止、最后一帧保存与画布载入。载入的PNG保存相机号、实际曝光和文件SHA-256；载入新帧后清空旧ROI及测量记录。
- 已接入按记录类型采集硬件点位：直径/圆柱度/跳动为轴5；孔径为轴2、轴5、相机1；粗糙度为轴1、轴5、相机2；长度/角度/圆弧半径为轴5、相机0。保存UTC采集时间、规划与编码器原始pulse，并校验工程中的类型、轴集合和相机对应关系。
- 相机采集与轴运动互斥；轴仍在运动、相机仍在采集或设备不可用时拒绝点位采集且保留旧值。相机停止未确认时阻止关闭编辑器。曝光编辑值不会被周期状态刷新覆盖。
- 相机与设备点位基础版已取得用户的Release构建和离线界面验证。直径光幕扩展随后也已通过`Release|x64`构建、光幕状态区离线检查、无硬件时不产生伪值及旧主窗口回归。三相机真实采图、光幕OUT1与补偿精度、四类真实点位及失焦停止仍需连接硬件验证。
- 2026-09-16用户反馈：`Release|x64`构建成功；离线设备点位页正常；工程载入后的画布空态文字已消失；相机选项可正常切换；工程重新打开、旧相机页和关闭窗口正常。现场没有连接硬件，因此相机0/1/2采图尺寸、PNG和四类真实点位仍未验证。
- 同次反馈发现图形化窗口无法切回主窗口。窗口调整为独立非模态窗口后，用户确认主窗口切换、图形化窗口拖动和最小化操作正常。失去焦点时停止图形化窗口启动的轴运动和相机采集，停止联动仍待硬件验证。

### E. 后续顺序

1. 编译并验证当前相机采集与设备点位候选版；硬件可用时同时完成低速轴控制安全验收。
2. **直径测量（候选版离线已验证，待硬件验证）**：设备点位页读取既有光幕线程的OUT1有效缓存，保存轴5点位、原始OUT1、原补偿函数结果和采样时间；离线时不产生伪值。真实OUT1、补偿精度、刷新稳定性和断线恢复留待现场验证。
3. **孔径测量**：接入测孔相机图像、孔轴点位、光幕轴点位、曝光和旧表单中的“均布个数”；逐型审计旧`cam1Picture_holeAlgorithm*`。旧源码的`holeNumber`实际由“均布个数”输入，不应解释为H0/H1位置序号。
4. **长度测量**：接入远心相机特征线、标定系数及跨图轴位移关系。
5. 处理粗糙度、圆柱度、跳动的硬件采样流程，然后接入程序生成、自动测量、现场验收、旧电脑部署和离线说明书。

孔径配置基础版已增加“孔均布个数”：仅选择孔径时可编辑，新建或更新孔径记录时必须大于0，并随工程JSON保存恢复；旧工程缺少该字段时按“未设置”兼容载入。用户已确认`Release|x64`成功、类型启用与必填拦截正确、数值6保存恢复正确，并确认改为长度后工程仍可保存恢复。孔径图像算法尚未接入。

## 节点 AM-GUI-20260914-01：图形化轴控制与布局基线

- 记录日期：2026-09-14。
- 工作仓库：`E:\331Software_Backup\_github_upload`，分支`main`。
- origin：`https://github.com/lonexlxlx/AxisMeasurement.git`。
- 记录时本地HEAD：`d6d4fca`（界面优化1）。这是本轮改动之前的提交，不是本节点的提交号。
- 记录时本轮源码、工程文件和文档仍有未提交修改，新增控制适配头文件及测试文件尚未跟踪。用户负责提交并上传；本文没有确认推送完成，也没有创建Git tag。
- 上传提交：`c412de1`（`增加运动控制部分`）；当前远端`origin/main`历史包含该提交。节点名称为文档标识，不等同于已存在的Git tag。

### 本节点包含的功能

1. 图形化窗口布局：左侧轴控制与图形列表、中央图像/ROI、右侧属性/配置/点位、底部流程与结果，顶部常驻停止和全部轴急停。
2. 轴1/2/5/6/7选择、Jog正负方向、绝对点位、速度及目标输入、使能与关闭使能、规划/编码器位置及轴状态显示。
3. 新入口复用控制卡连接，以显式轴号执行命令，不改写旧手动页的当前轴。采用应用模态和命令检查防止竞争操作；自动测量/回零运行期间限制进入。
4. Jog释放/窗口失焦停止；停止绑定发起轴；启动响应不确定时仍保留运动关联；停止失败时阻止关闭。停止栏独立于试测时禁用的编辑区。
5. 临时预览结束：`kManualLayoutPreview=false`、`kGraphicalAxisLayoutPreview=false`。用户已确认图形化面板的模式切换；无设备时恢复输入与运动入口限制。
6. 保留此前圆弧半径和双ROI夹角试测代码；单ROI倒角仍为待实现需求，目标为倒角斜边与相邻主体边之间的夹角。

### 验证结论及边界

| 项目 | 本节点证据 | 结论范围 |
|---|---|---|
| Release x64构建 | v142、Qt 5.14.2；`x64/manual_preview_build.log`记录成功输出EXE | 编译链接通过 |
| 离线自动回归 | `tests/run_graphical_axis_panel_test.ps1`执行通过 | 无设备禁用、就绪后模式控件切换、Jog释放、固定轴停止、失焦、绝对点位命令、启动响应不确定、停止失败关闭保护、急停及独立停止栏 |
| 用户交互确认 | 用户对图形化运动模式切换反馈“确认”，随后恢复限制 | 本次界面交互确认，不扩大为整体硬件验收 |
| 布局检查 | 离线窗口截图`x64/graphical_axis_offline.png` | 已检查当前尺寸下控件可见、文字显示；不代表所有分辨率/DPI均通过 |
| 真实轴运动 | 本轮未连接硬件验证 | 方向、限位、急停减速度/停距、垂直轴保持、到位精度、断线响应待现场验证 |
| 检测算法 | 本轮未运行真实算法回归 | 历史圆弧/双ROI角度验收记录保留，不据此声称新版全算法回归通过 |

当前状态读取仍在主线程每200ms调用SDK。上线现场运动前必须检查慢响应/断线是否阻塞界面；若阻塞，先改为串行后台通信并验证停止调度，不能只提高刷新频率。软件全部轴急停保留伺服使能，其效果需现场确认。点位目标为绝对pulse，速度为pulse/ms，不等同于毫米位移。

### 上传与回退记录

- 本次提交需包含已修改的工程注册、主窗口/辅助窗口/编辑器代码及文档，特别不要遗漏未跟踪的`myCode/head/graphical_axis_backend.h`和`tests/`下两个测试文件。
- 建议将本节点文档与本轮实现一起提交。上传后补记准确提交号；只有核对远端包含该提交后才标为“远端节点已确认”。
- `x64/`被Git忽略：EXE、构建日志和布局截图不会随源码自动上传。若需要完整发布包，另行归档运行依赖和上述验证证据，不能把源码推送当作运行环境备份。
- 后续开发以本节点提交为回退基线。在独立目录检出节点并使用匹配的依赖重建；不要覆盖仍有未提交工作的目录，也不要将后续硬件点位/配置用旧版本静默覆盖。

## 总体范围清单（原规划；执行状态以AM-GUI-20260915-02为准）

### 1. 检测参数与失败诊断（已接入，实图回归待完成）

- 核实当前圆弧/直线流程的固定Canny、分段、长度筛选和合并参数，建立每条记录自己的算法参数；默认保持当前已验收路径，参数范围依据源码和样图验证，不预设万能值。
- 首先暴露影响小倒角的边缘阈值、最短轮廓长度等必要参数；在结果状态中区分无边缘、筛选后无候选、多候选、拟合失败及许可错误。参数变更使旧结果失效。
- 收集普通边、短倒角、低对比度、干扰边和空白ROI的可重复图像与参数记录；缺少实物图像时可使用明确标注的合成图做流程测试，不能据此声称实物精度通过。
- 通过条件：默认参数的圆弧/双ROI角度回归通过；不同记录参数不串用；失败原因能定位到阶段；修改后不显示旧成功结果。许可或运行依赖问题单独记录为环境问题。

### 2. 单ROI框选倒角（候选版已接入，稳定性闭环待完成）

- 在角度记录中增加单ROI模式，保留双ROI模式。提取倒角斜边和目标相邻主体边，分别拟合并显示参与计算的两条线与角区。
- 先完成唯一有效边对的计算闭环，再增加多候选列表选择；在候选选择完成前，多候选明确失败，不自动取最长两条。小倒角不得因固定长度阈值被静默滤除或被合并为主体边。
- 保存模式、候选来源、算法参数、角区及失效关系；只选择算法结果，不允许拖动拟合线后继续冒充检测结果。
- 通过条件：不同朝向、普通/小倒角、补角、第三条边干扰、无候选、选择与失效恢复均可解释；原双ROI角度/圆弧回归通过。实物误差与重复性另行验收。

### 3. 工程保存与恢复（主流程已获用户验证，空态文字修复待复测）

- 建立有版本号的JSON工程，保存图像引用、必要ROI、记录、算法参数、候选选择语义和测量顺序；为设备点位预留明确的来源/单位/未采集状态。
- 文件保存采用失败可恢复的写入方式；载入损坏文件或缺失图像不覆盖当前工程。重新载入时验证引用关系和版本兼容，不把历史结果直接当作本次有效测量。
- 通过条件：保存—关闭—打开后图形、记录、参数、顺序一致；无效引用和路径问题可定位，能保留原工作。该阶段不等同于程序生成完成。

### 4. 图像采集与设备点位关联

- 对接现有相机采集，明确相机号、曝光、图像更新导致的结果失效；新增图像必须来自实际采集，失败时不显示旧图冒充新图。
- 在记录中区分控制卡原始坐标、补偿/换算后坐标、手动填写与硬件读取，并记录时间与来源。移动到位不自动意味着已采图或已记录点位。
- 无硬件先实现接口、状态和保存校验；真实相机及各轴位置由现场验证。
- 通过条件：来源和单位无歧义，缺失/失败不产生伪值，记录与相机及ROI正确关联。

### 5. 其余测量类型逐项接入

- 优先直径、孔径和长度：直径来自光幕传感器；孔径来自测孔相机并关联孔轴/光幕轴；长度来自远心视觉特征关系，跨图时还依赖轴位移。三者分别建模和验收，不共用一个ROI算法入口。之后再推进粗糙度、圆柱度、跳动。
- 长度先明确测量对象和基准关系，不能使用ROI内拟合线的可见长度代替零件长度；像素转毫米需对应标定。螺纹升角继续核实，不用中径函数替代。
- 每种类型分别完成配置—试测—结果—失效—保存的闭环与回归，不以一项通过代表全类完成。

### 6. 程序生成与自动测量集成

- 将稳定的工程记录映射到现有表单/源码生成链，先核实旧生成器能否表达新参数、ROI及角度模式；不能表达的类型应明确拒绝导出或补齐适配，不能降级成错误程序。
- 生成前检查标定、必要设备点位、文件依赖和引用完整性；输出UTF-8源码。自动测量列表仍需按现有Visual Studio集成流程注册和重新编译，不能宣称导出即用。
- 通过条件：生成源码编译通过、程序号及分派正确、离线启动回归通过；真实自动运动和测量留待现场验证。

### 7. 现场验收与正式交付

- 轴控制现场验收在硬件可用时尽早插入，优先于真实运动使用；无需等待其他算法全部开发完，也不阻塞无硬件期间的离线开发。
- 分设备连接、空载低速运动、相机/标定、标准件误差与重复性、完整自动流程、旧电脑部署和回退逐层验证。
- 完成可打印PDF及可编辑源文档的离线现场说明书，包含操作截图、参数记录表、故障排查、停止条件、验证证据和回退方法。最终按钮与路径以发布版为准。
- 发布条件：所有目标功能对应的验收结论明确；未通过的项有明确禁用或交付限制；运行依赖、合法许可、工程源码和说明书完整归档。

## 每个后续节点的固定记录方式

记录“目标与范围 → 修改文件 → 构建结果 → 自动/人工验证 → 未验证项 → 提交号 → 下一项”。一次推进一个可验证闭环，相关检查通过后再扩大范围；硬件或数据缺失时推进不依赖它的工作，不填模拟成功值。用户反馈改变需求时同步更新本文件和需求文档，历史事实保留，已失效的计划明确标注。
