# AxisMeasurement 面板功能测试指南

## 单ROI倒角首版验收（2026-09-14，待实图执行）

1. 打开有清晰倒角的原图，画一个包含倒角斜边、目标相邻主体边和角点的矩形或圆ROI。测量类型选“角度”，角度选取选“单ROI：框选相邻角 / 倒角”，填写特征号，新增记录；必要时点“选择/重选包含相邻角的ROI”再选框。
2. 在检测参数页确认最短轮廓是否小于倒角边长（原图像素）。默认20 px可能排除短边，可先尝试6–10 px；平滑过强可适当降低。90%点线偏差默认2.5 px，允许少量毛刺或圆角过渡点；角点间隙默认20 px，以允许过渡两侧的拟合线延长相交。输入后点击“应用检测参数”，再试测。上述数值是调试起点，不是精度保证；单ROI不使用合并距离。
3. 唯一相邻边对应直接显示角度、绿色原始边缘及橙色拟合线/角区。检查选中的确为倒角斜边与目标主体边。切换较大补角需“更新选中记录”并重新试测；较小角与补角之和应为180°。
4. 将ROI扩大到包含多个角：应显示待选择且无数值；在测量配置“候选边对”逐项选择并观察叠加，确认目标后采用。选择真实算法候选不能拖动拟合线；边对唯一不代表一定选中了所需物理特征。
5. 空白、仅一条边、平行边、交点远离两边端点、圆弧及干扰噪声分别试测：无合理边对时不得输出0或沿用旧值。拍下完整诊断；大角点间隙可能接纳非目标边，不应盲目增大。
6. 移动/缩放/旋转ROI、应用参数、重关联、删除ROI、更新角度模式后检查角度与候选清空；切换记录应恢复各自已提交模式和参数。未提交的模式输入不改变已保存记录。单ROI时ROI2入口禁用，提交双ROI后恢复。
7. 回归原圆弧和双ROI角度；记录样图、原图像素尺寸、参数、目标边、诊断和测量重复值。仍显示“未判定”，本阶段不进行标定精度或真实运动验收。

8. 稳定性复测时固定同一张静态原图、同一ROI和同一组已应用参数，连续试测至少10次，结果应每次一致；随后只分别向四个方向移动ROI少量像素，确认只要仍包含角点和两段足够长的直线就能输出同一目标边对。诊断应显示“直线分段→共线合并→长度筛选→有效直线→相邻边对”。若固定输入仍时成时败，应按程序状态问题处理；若只在移动ROI后失败，依据各阶段数量判断边缘截断、长度不足、偏差过大或交点间隙不足。

本机离线测试不加载HALCON算法，也不连接设备；实图验收需匹配工程的HALCON 18.11运行库与有效许可。

若旧版本显示“单ROI直线拟合：HALCON 2: Illegal operation on tuple”，这是`cont_approx`属性读取类型错误，不是通过调整阈值可解决的无边缘问题。新版单ROI已改为直接按直线段分割，不再读取该属性；仍保留拟合输出元组保护。

> 临时预览已结束（2026-09-14）：用户确认图形化编程左侧运动模式切换后，已关闭kGraphicalAxisLayoutPreview并恢复设备就绪限制；主窗口手动页的临时预览也已关闭。无硬件时轴选择、模式和运动参数恢复禁用。该确认仅针对界面交互，不代表真实轴运动验收通过。

> 面向不熟悉本软件的测试人员。按"无硬件 → 有硬件 → 自动测量"三个阶段推进，每个面板给出操作步骤、测试数据和**预期结果**（预期结果不对 = 有问题，记下来）。
>
> 适用于 P2 布局重构后的版本（面板收纳在页签里、左右分栏可拖动）。

---

## 第 0 步：测试前提（必读）

1. **程序必须能启动**：需要 `x64\Release\` 下有全部 DLL（大恒 SDK 已装；HALCON 18.11 的 `halconcpp.dll` 还没装的话程序起不来，先装）。
2. **登录**：用户名 `XJTU`，密码 `121314`。**输完每一栏按一下回车**再点登录（输入框靠回车提交，直接点按钮可能读到空值）。
3. **一个重要概念**：这套软件的绝大部分功能都有"设备门槛"——没连相机/运动控制卡/光幕时，"打开设备"会失败，**手动控制页、程序选择、开始测量全都保持灰色**。这不是 bug，是设计如此。所以测试分两阶段。

---

## 第一阶段：无硬件就能测的（现在就能做）

### 1.1 登录窗（P1 改版）

| 操作 | 预期 |
|---|---|
| 启动程序 | 弹出无边框卡片式登录窗，背景图，可按住空白处拖动 |
| 密码框输入 | 显示圆点掩码 |
| 输错密码点登录 | 提示"账号密码错误"，窗口不关 |
| 输对（XJTU/121314，各按回车） | 登录窗关闭，主窗口出现 |

### 1.2 主窗口整体布局（P2 重构核心验证）

先确认整体形态，再逐面板测：

| 检查点 | 预期 |
|---|---|
| 自动测量页布局 | 顶部设备控制条；左栏=程序选择+光幕实时显示+"顶尖/旋转/光幕位置"页签；中间=图像区；右侧=测量统计+测量结果 |
| 底部状态栏 | 右侧显示 **●红 设备未打开** + 提示文字"设备未打开，请先打开设备！" |
| 拖动分隔条 | ①主窗口中间竖条（左页面/右结果栏）②左栏与图像区之间竖条 ③悬停变蓝、可拖动、拖不没 |
| 缩放窗口 | 拖窗口边缘任意缩放，最小到 1440×900 以下不了；控件跟着伸缩不重叠 |
| 菜单栏"测量模式选择" | 有"自动测量模式/手动控制/二次开发辅助"三项 |

### 1.3 页签收纳测试（点一遍每个 Tab）

**自动测量页左下角的"顶尖 / 旋转 / 光幕位置"页签**——只有一个 Tab，点它确认展开后有：顶尖上移/下移、转台慢转/快转、光幕上移/下移 6 个按钮（P1 后带图标）。按钮无硬件时点击会无反应或提示设备未开，**不闪退即通过**。

**手动控制页的 5 个页签**（无硬件进不去，见 2.2，此处先跳过）。

### 1.4 设备控制条（自动页顶部）

| 操作 | 预期 |
|---|---|
| 点"打开设备" | 无硬件 → 状态栏提示"设备未全部打开，请检查设备连接！"，程序选择下拉框保持灰色，红灯不变 |
| 点"紧急停止" | **P2 新效果：全窗口红色边框闪 3 下（约 1.2 秒）**，然后恢复正常；不崩溃 |
| 观察"程序"下拉框 | 灰色不可选（设备没开） |

### 1.5 光幕实时显示面板（左栏中部的仪表盘）

| 检查点 | 预期 |
|---|---|
| "实测值"数字 | 大号等宽字体（Consolas 24pt）绿字浅绿底，居中 |
| "直径"数字 | 蓝字浅蓝底大号数字 |
| 无硬件时数值 | 不更新（显示初始占位），这是正常的 |

### 1.6 测量统计面板（右栏上部）

| 操作 | 预期 |
|---|---|
| 检测员姓名输入 `测试员01` | 可输入 |
| 零件编号输入 `TEST-001` | 可输入 |
| 检测零件数/OK数/NG数/良品率 | 显示 0/0/0/-- |

### 1.7 测量结果面板（右栏下部）

| 操作 | 预期 |
|---|---|
| 表格外观 | 表头灰白、数字列等宽粗体字体（Consolas） |
| 点"创建数据表"按钮 | 无硬件也能建库：`x64\Release\measureData\axisDatabase.db` 里出现 61 张按零件图号命名的表（可用 DB Browser for SQLite 打开验证）；再次点击不报错 |
| "保存测量结果/清空测量结果" | 灰色（测量没跑完不能保存，正常） |

### 1.8 二次开发辅助窗口（菜单 → 测量模式选择 → 二次开发辅助）

| 操作 | 预期 |
|---|---|
| 打开 | 弹出 sdk_assist 窗口，顶部工具栏有"打开图形化编程" |
| 点"打开图形化编程" | 弹出图形化编程编辑器：工具栏图标在上文字在下（V/P/L/R/C/A/F 快捷键需点一下画布区域才生效）；画布深灰底，未开图时中间有灰字提示 |
| 点"打开图像"选一张 bmp | 图像载入，空态提示消失 |
| 画矩形/圆/圆弧 | 能画、能选中、能拖动；图形不出图像边界；圆弧=依次点三个点 |
| ⚠️ 点位记录相关按钮 | **已知缺陷：未连硬件时记录直径点位曾闪退（SDK-OFFLINE-001），谨慎点击，重要数据先保存** |

---

## 第二阶段：连上硬件后测的

> 硬件=大恒相机×3 + 固高运动控制卡 + LS9 光幕 + 顶尖/转台机构。连好后从这继续。

### 2.1 打开设备（一切的前提）

1. 点"打开设备" → 预期：状态栏变 **●绿 设备已打开**，"程序"下拉框、"设备回零"、"开始测量"全部解除灰色。
2. 点"设备回零" → 预期：各轴回原点，轴状态指示灯变化，期间按钮不可重复点。

### 2.2 手动控制页（菜单 → 测量模式选择 → 手动控制）

设备已打开才能进。进去后重点测 P2 收纳的 5 个页签 + 轴监控：

**轴监控（常驻左侧）**
| 检查点 | 预期 |
|---|---|
| 8 个指示灯 | 使能/负限位/驱动报警/正限位/IO急停/跟随误差/运动状态等，绿色=正常，异常变红 |
| 实时位置 | 数值随轴运动刷新 |

**运动轴设置页签**
| 操作 | 预期 |
|---|---|
| 轴号下拉换轴 | 下面的点位/Jog 参数跟着切换到该轴的参数 |
| "伺服使能"→"关闭伺服" | 指示灯切换，轴上电/掉电 |
| "位置清零" | 当前轴位置显示归 0 |
| "紧急停止" | 轴停 + **P2 闪红边框** |
| "一键回原点" | 轴回零 |

**Jog运动页签（按住走、松手停）**
测试数据建议从小值开始：
| 参数 | 建议测试值 | 说明 |
|---|---|---|
| 速度 | 50 | 脉冲/ms，先慢 |
| 加速度/减速度 | 0.5 | 脉冲/ms² |
| 平滑系数 | 0.5 | 范围 0~1 |
| 正向/负向 | 按住看轴动 | 伺服使能后才有反应 |

**点位运动页签（走固定步长）**
| 参数 | 建议测试值 |
|---|---|
| 步长 | 1000（脉冲） |
| 速度 | 20 |
| 加/减速度 | 0.5 |
| 平滑时间 | 10（范围 0~50） |
| 点"启动运动" | 轴走一个步长后停，轴监控位置变化对应 |

**相机采集页签**
| 参数 | 建议测试值 |
|---|---|
| 相机选择 | 3 台轮换测 |
| 曝光时间 | 400（μs，默认值，范围 0~30000） |
| 增益 | 0（范围 0~24dB） |
| 开始采集 | 右侧"相机图像"区出实时图 |
| 结束采集 → 图像保存 | measureData 下出现 bmp |

**光幕传感器页签**
| 检查点 | 预期 |
|---|---|
| OUT1~4 数值 | 20pt 绿色等宽大数字，挡光幕后数值变化 |
| 补偿前数值 | 琥珀色大数字 |
| 跳动数据采集 | 光幕下移采集一圈数据后回到原位（动作流程完整） |

### 2.3 顶尖/旋转/光幕位置页签（自动页左下，设备打开后启用）

| 操作 | 预期 |
|---|---|
| 顶尖上移/下移 | 顶尖机构动作（注意工位安全！） |
| 转台慢转/快转 | 转台旋转，快转速度明显更快 |
| 光幕上移/下移 | 光幕传感器支架动作 |

---

## 第三阶段：自动测量全流程

1. 设备打开 + 回零完成。
2. "程序"下拉选一个（建议先选 `螺纹教学` 或 `1_WZ10451080-370`）→ 预期：右侧"零件装夹方式"显示对应图号。
3. 点"开始测量" → 预期：
   - **进度条平滑增长**（P2：OutCubic 动画，不是跳变）；
   - "测量程序状态"文字逐步变化（走到哪一步显示哪一步）；
   - 中间图像区显示采图；
   - 过程中弹确认对话框时，点"继续测量"走下一步，点"检测取消"中止。
4. 测量跑完 → 预期：
   - 结果表逐行填入：数值列等宽粗体，**OK 行绿字、NG 行红字浅红底、临界行橙字浅橙底**（一眼可辨）；
   - 测量统计的检测零件数 +1、OK/NG 计数、良品率刷新；
   - "保存测量结果"解除灰色，点击后数据入库 + 导出 xlsx（`measureData\Report\` 下）。
5. 中途点一次"紧急停止" → 预期：全窗口闪红、机构停、程序状态提示中止。

---

## 测试数据速查卡

| 位置 | 字段 | 测试值 |
|---|---|---|
| 登录 | 用户名/密码 | XJTU / 121314 |
| 测量统计 | 检测员姓名 | 测试员01 |
| 测量统计 | 零件编号 | TEST-001 |
| 程序选择 | 程序 | 螺纹教学（首次建议） |
| Jog | 速度/加减速/平滑 | 50 / 0.5 / 0.5 |
| 点位 | 步长/速度/平滑时间 | 1000 / 20 / 10 |
| 相机 | 曝光/增益 | 400 / 0 |

## 已知坑（测到这些不算新 bug）

| 现象 | 原因 |
|---|---|
| 无硬件点"手动控制"菜单没反应 | 代码要求设备全部打开才允许进手动页 |
| 无硬件"打开设备"报错、按钮全灰 | 预期行为 |
| HALCON 相关报错 #2042 | 许可证问题，与界面无关 |
| 二次开发辅助里离线记录点位闪退 | 已知缺陷 SDK-OFFLINE-001，勿在无硬件时点 |
| 图形化编辑器撤销/重做灰色 | 功能尚未开发（占位按钮） |
| 偶发卡顿/残影 | 历史遗留，正在排查 |

## 图形化窗口轴控制（2026-09-14新增）

入口：二次开发辅助 → 打开图形化编程。先做以下离线检查，再安排现场运动验证。

| 离线操作 | 预期 |
|---|---|
| 打开图形化窗口 | 左侧上方轴控制、下方图形列表；中央画布；右侧属性/配置/点位；底部测量流程 |
| 未连接设备 | 状态为未连接/未采集；不能使能或移动轴，不显示0冒充真实位置 |
| 拖动左侧上下分隔条和三栏分隔条 | 画布与列表大小可调整；控制区内容不足时可滚动，顶部停止/全部轴急停始终可见 |
| 打开本地图像并操作ROI、记录 | 原编辑流程仍可操作；本地图像和相机图像更换都会清空原ROI与测量记录 |
| 切换到主窗口，再返回图形化窗口 | 两个窗口可正常切换；图形化窗口可拖动、最小化和恢复；失去焦点时停止由该窗口启动的轴运动和相机采集 |
| 关闭再打开图形化窗口 | 布局与控制接口仍正常，没有重复面板 |

现场验证须先确认物理急停、限位和轴周边安全，按现有规程空载、低速逐轴进行。默认速度1 pulse/ms并非已验证安全值，应按现场设备选择；新面板目标位置是绝对pulse坐标，不是增量距离。

1. 在主窗口打开设备，再进入图形化窗口；确认1/2/5/6/7轴名称、规划与编码器位置、使能、运动、报警和限位状态与设备对应。
2. Jog分别短按正负方向，核对方向；释放后停止。切换到其他应用时停止，不应在恢复窗口后自动重启；移动中轴号和参数不可修改。
3. “绝对点位”填写已确认的小范围目标，点击移动并核对到位。加减速沿用原轴参数；不要把目标0误认为不移动。
4. 分别验证“停止当前轴”和“全部轴急停”。软件急停使用控制卡急停减速度并保留伺服使能；垂直轴保持、减速度和停距必须现场确认。
5. 移动中关闭窗口或点击试测，应先请求停止；未确认停止时拒绝关闭/试测。模拟通信异常需先用物理急停保证设备安全，确认失败提示和界面响应，不因软件按钮发出命令便认定已停。
6. 自动测量/回零运行期间不能打开图形化窗口；旧窗口运动未结束时，新移动入口应拒绝命令。全部结束后关闭图形化窗口，回归旧手动控制页。

自动离线回归：主工程Release x64编译完成后运行 `tests/run_graphical_axis_panel_test.ps1`。测试后端不连接硬件，覆盖Jog释放、失焦、固定轴停止、启动响应丢失、停止失败关闭保护、常驻停止栏，以及模拟相机的断开/就绪/采集中状态、开始/停止命令和最后一帧自动缓存载入；不验证真实SDK通信、运动精度、真实相机曝光/启停或检测算法。

## 发现问题怎么记录

### 检测参数与阶段诊断（2026-09-14新增）

1. 打开本地图像，新增圆弧半径或角度记录并完成ROI关联；进入右侧“检测参数”。确认默认值依次为2、20、60、20、自动图宽/2、90。双ROI共用这组参数。
2. 默认参数试测，与此前相同图像和ROI结果对照。圆弧单位仍为px、角度为°，均保持未判定。本轮不更改标定或合格判定。
3. 调整一个参数后点击“应用检测参数”：旧值和叠加应清空，状态提示旧结果失效；特征号、公差、两项ROI关联应保持。仅输入数值不提交时，试测仍使用记录中的已提交值。
4. 新增第二条记录，使用不同参数，来回选择确认各自保留。点击“恢复默认（未提交）”后切换记录再返回，应仍恢复已提交值；想保留默认值需再点击应用。
5. 将低阈值设置为不小于高阈值，或将正数最长长度设得小于最短长度后应用，应提示拒绝，原记录不变。最长长度0的有效上限为图像宽度的一半；若小于最短长度，应提示调整长度参数。
6. 使用空白ROI、仅含短边的ROI及多边干扰ROI分别试测。完整诊断在本页及结果表执行状态的悬停提示中显示，应能区分无边缘、分段失败、长度全被过滤、多个合并候选和拟合失败；实际出现哪个阶段由图像决定，不能预设某一空白图必达特定阶段。
7. 小边缘先只减少最短长度，观察筛选后计数；如果提取阶段就是0，再检查对比度和边缘阈值。如果出现多个候选，不自动选择最长一条。改变阈值/合并距离也可能引入噪声或错误合并，必须观察叠加是否对应目标。
8. 双ROI任一侧失败，应显示对应ROI编号；恢复有效ROI或参数后可重试。圆弧和角度互相回归，移动/旋转ROI后旧结果仍须失效。

本轮自动测试只覆盖参数校验、记录交互和轴控制，没有运行HALCON检测。实图试测需匹配工程的合法运行环境；2042应明确显示许可不可用或已过期，不能当作ROI或阈值错误处理。反馈请带图像/ROI、记录号、已提交六项参数、完整诊断和实际叠加截图。

### 工程保存与恢复（2026-09-15主流程已验证，空态修复待复测）

本项请先编译`Release|x64`，再用测试图像的副本操作，不修改生产图像或唯一原图。

1. 打开一张本地图像，分别创建点、直线、旋转矩形、圆和三点圆弧；建立圆弧半径、双ROI角度和单ROI角度记录，设置不同特征号、公差和检测参数。记录图形ID、关联关系及顺序。
2. 点击“保存配方”，选择`.axisproj.json`。继续移动一个ROI后按`Ctrl+S`，关闭窗口并重新进入图形化编程，再点“打开配方”。预期图像、图形位置/旋转、稳定ID、记录顺序、关联、公差、参数及角度模式一致。
3. 打开后所有历史测量值、检测叠加和候选选择应显示为需重新试测；不得把保存前的成功角度直接当作当前有效结果。重新试测后再确认候选。
4. 将配方JSON和图像副本一起移动到另一个文件夹且保持二者相对位置，再打开配方。预期仍可恢复；只移动JSON而使相对图像路径失效时，应报告无法读取图像并保留当前已打开配方。
5. 复制一份工程与图像作为破坏性用例。在不改变图像宽高的情况下修改或替换图像内容，再打开对应工程；预期提示图像内容已变化。若图像在打开后被外部改写，再保存当前工程，也应拒绝并要求重新打开图像核对ROI。
6. 分别把副本JSON改成非法JSON、未知版本、重复图形ID、越界图形、缺少必填数值、引用不存在图形的记录。每次都应给出明确错误，并保持测试前当前画布和记录不变。
7. 当前“设备点位”仍为未采集状态；工程恢复不应显示0或其他数值冒充硬件位置。直径、孔径和长度的真实采集/测量尚不属于本项验收。

反馈请提供：Release构建结果、成功恢复前后截图、JSON与图像所在路径、失败提示原文，以及发生问题的第几步。本项通过不能替代单ROI角度10次重复性测试。

2026-09-15用户反馈功能均通过，截图确认图像、4个图形及3条测量记录恢复，图像和图形可移动，旧结果正确标记为需重新试测。遗留显示问题为画布中央仍出现“尚未打开图像”；源码已补充工程载入成功后隐藏空态提示。下一次编译只需重点确认该文字消失且正常打开本地图像时空态提示仍能隐藏。

### 相机采集与设备点位（2026-09-15候选版）

本轮先编译`Release|x64`。若失败，返回错误列表中第一组完整错误（包含文件、行号和错误码），本轮其余步骤暂停。

1. 不打开设备进入图形化窗口，点击“相机图像”。预期跳到“设备点位”页；相机状态显示未连接，开始/停止/载入按钮不可操作；选择一条测量记录后，记录点位按钮不可操作。确认重新打开配方后画布中央“尚未打开图像”已经消失。
2. 在主窗口打开全部设备，进入图形化窗口。依次选择相机0远心、相机1测孔、相机2粗糙度。每台相机填入该设备当前安全曝光，确认输入值保持不被状态刷新改回；点击开始，等画面稳定后点击停止，再点“载入最后一帧”。预期不弹出PNG保存窗口，画布载入对应图像并显示尺寸和实际曝光，中央空态文字不出现。
3. 每台相机采图后关闭图形化窗口，再在原主窗口相机页对同一相机执行一次开始/停止。预期旧相机页仍可正常采集，且图形化窗口没有遗留显示线程。
4. 三台相机都完成一次采集并保留最后一帧后，建立四条代表记录并逐条点击“记录选中记录的当前设备点位”：直径应显示轴5、光幕OUT1原始值和补偿直径且无相机；孔径应显示轴2、轴5、相机1曝光；粗糙度应显示轴1、轴5、相机2曝光；长度或角度应显示轴5、相机0曝光。记录时相关轴必须已停止，相机也必须停止；记录应显示UTC时间。
5. 保存配方，关闭并重新打开。预期四条点位及单位pulse完整恢复，历史试测结果仍要求重测。把其中一条记录改成另一测量类型并点击“更新选中记录”，预期旧点位清空；“清除选中记录的设备点位”也应立即清空并在保存后保持。
6. 启动一台相机后直接关闭图形化窗口。预期软件先停止该相机再关闭；若线程未在1.5秒内停止，窗口应保持打开并显示错误。再确认采集期间不能打开本地图像或工程。
7. 打开图形化窗口后切换到主窗口，再切回；分别拖动、最小化和恢复图形化窗口。预期窗口操作正常。若由图形化窗口启动了轴运动或相机采集，切换离开时应先停止，返回后不会自动恢复运动或采集。

请返回以下信息：

- `Release|x64`编译成功，或第一组完整编译错误。
- 离线设备点位页截图。
- 相机0/1/2各自使用的曝光、停止后的尺寸/曝光状态、保存PNG路径；失败时返回相机号、按钮顺序和提示原文。
- 四条代表记录的设备点位页及结果表截图；重新打开工程后的同一区域截图。
- 旧主窗口相机页开始/停止是否正常，以及关闭图形化窗口时是否出现卡住、残留画面或停止超时。
- 主窗口切换、图形化窗口拖动、最小化和恢复是否正常；切换窗口时轴与相机是否按预期停止。

### 直径光幕采样（2026-09-16候选版）

当前结果：用户已确认`Release|x64`成功、光幕状态区正常、离线直径点位未产生伪值、旧主窗口正常。以下连接真实光幕的项目仍待现场执行。

1. 编译`Release|x64`。无硬件进入“设备点位”，预期新增“光幕传感器”状态区并显示未连接；创建直径记录后，“记录当前设备点位”保持禁用或明确拒绝，不得出现OUT1为0的已采集记录。
2. 现场连接全部设备后，观察光幕状态区至少10秒。预期OUT1原始值和补偿直径持续更新；主窗口原光幕显示仍正常，两个位置的数值在相同时间范围内一致。
3. 轴5停止后记录一条直径点位。预期结果表包含轴5编码器位置和`OUT1:原始值→补偿值mm`；保存、关闭并打开工程后数值、单位和时间保持一致。
4. 断开光幕或停止有效更新超过3秒，再尝试覆盖该直径点位。预期拒绝并保留原值；恢复连接并出现新样本后才允许重新记录。

反馈请返回：Release构建结果；无硬件光幕状态区截图；现场测试时连续观察是否稳定、主窗口与图形化OUT1/补偿值、保存恢复截图，以及断线时提示原文。当前没有光幕硬件时，只需先返回构建结果和离线截图。

### 孔径均布个数配置（2026-09-16候选版）

当前结果：用户确认`Release|x64`成功；类型切换启用状态正确；未设置时提示“孔径记录需要填写孔均布个数。该值对应旧表单的‘均布个数’，不是H0/H1拍照位置。”；均布个数6保存恢复正确；记录改为长度后工程仍能正常保存恢复。本节离线验证已通过。

1. 编译`Release|x64`。进入“测量配置”，在直径、长度和角度之间切换，预期“孔均布个数”不可编辑；切换到孔径后可编辑。
2. 选择孔径，保持“孔均布个数”为“未设置”，填写特征号并新增记录。预期明确提示需要填写均布个数，不新增记录。
3. 输入均布个数6并新增孔径记录；再选中该记录，预期配置框显示6。保存工程、关闭并重新打开，选中同一记录，预期仍为6。
4. 将该记录测量类型改为长度并更新，再切回该记录，预期均布个数不可编辑且内容不作为长度配置保存；工程仍能保存和重新打开。

请返回：`Release|x64`结果；步骤1是否按类型启用；步骤2提示原文；步骤3重新打开后的均布个数；步骤4工程保存与恢复是否正常。孔径算法和真实相机测量不在本轮测试范围内。

测量配置初始状态回归：首次打开图形化界面且默认测量类型为“直径”时，“角度选取”“候选边对”和“角度结果”均应不可编辑；切换到“角度”后，“角度选取”和“角度结果”应立即可编辑；切换回其他类型后再次不可编辑。候选边对仅在已保存的单ROI角度记录产生候选结果后可编辑。用户已确认`Release|x64`成功，首次打开默认直径时两项不可选，切换角度后可选，切换回孔径或长度后再次不可选；本项通过。

### 孔径单ROI原算法试测（2026-09-16候选版）

1. 编译`Release|x64`。失败时只返回第一组完整错误，包含文件、行号和错误码；成功后再继续。
2. 打开一张原软件曾成功测孔的相机1原图或本地原始测孔图。在目标孔外画一个圆形ROI；也可用矩形ROI。ROI应框住完整孔，边界尽量贴近孔的外轮廓但不要切掉上下孔边，并排除其他孔和强干扰边。ROI只是自动确定原算法上下测量位置，不参与圆拟合。
3. 新增孔径记录，填写特征号、均布个数和公差，测孔标定先保持`0.00691842 mm/px`。若新增时已经选中该ROI，记录会直接关联；否则选中记录后点击“重新关联图形”，再点击该ROI。
4. 点击“试测选中记录”。成功时画布应叠加检测到的上下边、两条拟合边和最终采用的跨边距离线。返回结果表毫米值、状态栏完整文字和画布叠加截图，并同时提供原软件对同一原图的孔径值。记录两者差值，不用公称值代替原软件结果。
5. 失败时返回状态栏和“检测参数”页中的完整阶段提示及截图，尤其保留“图像/ROI准备”“原测孔图像增强”“ROI上下边Metrology”“上下边Tukey拟合”或“孔径计算”等阶段。若为HALCON 8573，截图必须包含画布上叠加的两条实际搜索线，以判断搜索位置是否覆盖上下内孔边。
6. 保存工程并重新打开，确认ROI关联、均布个数和测孔标定保持；历史孔径结果应清空并提示重新试测。删除关联ROI后再试测，预期明确提示关联已删除。
7. 负向用例：不关联ROI、关联点/直线/圆弧，以及加载明确来自相机0或相机2的工程图像。预期均被拒绝且不产生孔径值。

请返回：`Release|x64`结果；测试图像路径；ROI和拟合叠加截图；图形化孔径值与原软件孔径值；成功状态全文，或失败阶段全文；工程恢复及负向用例结论。若手头没有原软件成功样图，本轮先完成构建、关联、保存恢复和负向用例，算法精度继续标记为未验证。

已知`3.5 mm`测试孔首轮结果：用户确认`Release|x64`成功，界面状态给出`229.795 px × 0.01913360 mm/px = 4.3968 mm`，相对已知值的精确差值为`+0.8968 mm`。随后诊断版在较大ROI上得到四组距离`255.819/273.939/247.419/272.837 px`、第2组最大、中点距`260.063 px`及拟合线长`49.574/56.155 px`；截图明确显示上拟合线落在灰色环带而下拟合线接近黑色内孔边缘。最大值只比中点距大约5.3%，本次主要误差是ROI按外框比例布置搜索线导致上下取到不同轮廓，标定影响仍需在取边一致后单独验证。

ROI适配层已改为先在ROI中央灰度剖面中寻找成对的内孔上下跃迁，再把原Metrology、Tukey拟合和四组距离计算放到定位结果上。用户确认该版`Release|x64`成功；移动圆ROI时，只要孔完整位于圆内，两条橙色拟合线均稳定落在黑色内孔上下边缘。当前测试图上的取边与ROI移动稳定性通过。该轮没有保留叠加截图、完整状态文字和重复数值序列，因此毫米误差转入标定与拍摄条件验证，但仍需标准件重复测量才能形成精度结论。

截图（含状态栏提示文字）+ 当时在哪个页签 + 点了什么按钮 + 输入了什么值 → 发给开发。界面布局类问题（控件重叠/错位/字体异常）优先级最高，因为刚做过 P2 布局重构。

### 单图双ROI长度基础回归（2026-09-20）

本节只验证ROI引导下的取边、拟合、长度公式、配置失效和工程恢复，用作模板跟踪接入前的基础回归；通过本节不代表旧程序的形状模板定位链已经接入。

1. 编译`Release|x64`。失败时返回第一组完整错误；成功后继续。
2. 打开一张相机0远心原图或本地远心样图，选择“长度”，确认“远心标定”可编辑且默认显示`0.01218603 mm/px`；切换到其他测量类型时该字段应不可编辑。
3. 在同一张图中分别用矩形ROI框住两条近似水平的目标边，新增长度记录，然后用“长度：选择/重选 ROI 1（基准边）”和“长度：选择/重选 ROI 2（目标边）”完成关联。两个ROI不能相同。
4. 点击“试测选中记录”。成功时应叠加两条拟合边和两边中点连线，表格显示mm值；状态栏包含两侧方向角、方向差、像素距离、标定和毫米结果。返回画布截图、完整状态和已知/原软件同图长度值。
5. 将任一ROI移到没有唯一边缘的位置，预期按ROI编号提示失败且不保留旧值；让两条边明显不平行或选择明显倾斜边，预期在“长度计算”阶段拒绝。
6. 保存并重新打开工程，确认两个ROI关联、远心标定和检测参数恢复，旧长度结果已清空。若工程图像明确来自相机1或相机2，试测应被拒绝。

请返回：`Release|x64`结果；远心标定字段按类型启用是否正确；成功叠加截图与完整状态；图形化长度值和已知/原软件同图值；两个失败用例结果；工程恢复结论。本轮不测试跨图长度或真实轴5补偿。

首轮结果：用户确认构建、字段启用、两个ROI恢复、远心标定恢复、检测参数恢复和旧结果清空均正常；截图中的长度记录输出`66.4885 mm`。截图时测量类型已切到“粗糙度”，表单远心标定显示`0.07000001 mm/px`，无法证明该数值就是长度记录试测时使用的已提交标定；像素距离、完整状态、五次重复结果和两个失败提示尚缺。补测时应选中长度记录，确认表单类型回到“长度”，把远心标定明确设为`0.01218603 mm/px`并更新记录，然后再试测并复制完整状态。

补测结果：标定固定为`0.01218603 mm/px`后，固定图像和ROI连续5次均为`11.5735 mm`，重复性通过。成功截图的另一ROI位置为`11.5747 mm`，方向诊断`ROI1=0.070°、ROI2=9.449°、方向差=9.379°`；倾斜边用例为`ROI1=0.015°、ROI2=14.962°、方向差=14.947°`，按“长度计算”阶段拒绝且表格测量值清空。固定输入重复性和不平行拦截通过；ROI移动鲁棒性、无边缘提示、已知尺寸精度及模板跟踪不在已通过范围内。

正式单ROI版本接入后，本节双ROI步骤只作为内部回归。面向用户的验收应改为：一次框选完整长度特征、建立记录、直接试测；画布自动叠加模板匹配位置、两条拟合边和长度连线。移动或重新采集目标后再次试测，不重新选择两条边，并核对匹配分数、两边诊断和长度结果。

### 单ROI模板长度候选版验收（2026-09-21）

1. 编译`Release|x64`。打开此前长度测试使用的远心图，画一个矩形ROI：一次框住完整被测长度特征及上下两条目标边。ROI内尽量只保留这两条近似水平边，并包含足够独特的轮廓供模板匹配。
2. 新增长度记录，关联该矩形。确认表格关联显示`单ROI:矩形_n`，“长度：选择/重选单ROI（完整特征）”可用，“长度：单ROI自动提取两条边”不可用。关联圆形ROI应被明确拒绝。
3. 设置远心标定`0.01218603 mm/px`并试测。首次试测会建立模板。成功时应叠加两条橙色拟合线和长度连线；状态包含`单ROI模板长度试测完成`、模板分数、边1/边2方向角、方向差和检测阶段诊断，状态栏末尾包含像素距离、标定和毫米结果。
4. 不移动图像和ROI，连续试测5次，五次毫米值和模板分数应一致。随后小幅移动ROI会使旧模板和旧结果失效；重新试测应重建模板，不能沿用旧数值。
5. 保存工程并重新打开。确认单ROI关联、远心标定、检测参数和模板已恢复，旧试测结果已清空；直接再次试测时应成功使用已保存模板，不要求重新框两条边。
6. ROI包含第三条以上横向边时，程序按图像Y方向排序，只在相邻间距存在明显唯一最大值时自动选择。状态应写明横向候选数、采用间距和次大间距；叠加必须落在预期两条边。若最大与次大间距接近，应在目标边拟合阶段明确失败。把ROI缩到缺少完整特征的位置应在模板匹配阶段失败；两条目标边明显不平行应在长度计算阶段拒绝。任何失败都不得保留旧长度值。

请返回：`Release|x64`结果；首次试测完整状态；图形化长度值、像素距离和模板分数；连续5次结果；工程重开后直接复测的完整状态；一个失败提示原文；成功画布叠加截图。当前编辑器更换本地图像会清空当前工程，因此本轮先验证同图模板恢复；跨图自动跟随留到相机采集/自动测量流程验收。

验收结果：用户确认最高分匹配版本`Release|x64`成功，原易出现重复匹配的ROI连续10次均定位同一台阶、长度一致，模板分数约`0.99`；工程重开后无需重新框选且匹配正确。随后增加多横边的相邻主间距选择，用户确认偏大ROI用例通过。单ROI长度同图离线稳定性通过；该结论不替代标准件毫米精度和跨图轴位移验收。

### 长轴段跨图长度验收边界

跨图长度分两阶段验收。离线阶段使用两张相机0原图，验证两端ROI能分别拟合目标边并返回起点、终点行坐标；没有真实轴5点位时必须提示点位未采集，不得输出0或伪造长度。硬件阶段再验证轴5补偿位移和最终公式。

### 双帧工程第一阶段测试

本阶段只验收帧集合、ROI隔离、记录所属帧和工程恢复，尚不验收跨图长度计算。

1. `Release|x64`编译。
2. 点击“打开图像”载入图A，画一个矩形ROI并新增一条长度记录。
3. 点击工具栏“添加帧”载入图B，在图B画另一个矩形ROI。
4. 用工具栏帧下拉框在帧1和帧2之间往返切换三次，确认每帧只恢复自己的图像和ROI，另一帧ROI没有被清空或串入当前帧。
5. 停留在帧2时选中帧1的长度记录，确认关联图形显示为“帧1/图形…”，点击试测应提示先切换到帧1；切回帧1后应能正常试测。
6. 保存工程，关闭并重新打开，确认两帧、两组ROI、当前帧和帧1记录关联均恢复，旧试测结果仍清空。
7. 如有此前保存的版本1工程，再打开一次，确认能正常载入；另存后应按版本2保存。

请返回：`Release|x64`结果；图A/图B各自ROI往返切换结果；帧2试测帧1记录的提示原文；保存重开后的帧数、当前帧、两帧ROI和记录关联恢复结果；版本1工程兼容结果。若失败，返回第一组完整错误或发生问题前后的操作顺序。

### 跨图长度端点配置与离线拟合测试

本阶段验证双端ROI、自动点位数据模型和两端离线拟合；最终毫米长度仍需两端真实轴5点位。

1. `Release|x64`编译。
2. 打开图A并添加图B，分别绘制一个矩形ROI。
3. 在图A新增长度记录，将“长度模式”设为“跨图长度（起点/终点）”。
4. 选中该记录，点击“确认起点ROI并采集点位”，再点击图A矩形。使用本地图像时，下方列表应显示起点未采集；不应生成0点位。
5. 切换图B，点击“确认终点ROI并采集点位”，再点击图B矩形。关联图形应同时显示起点帧1和终点帧2，设备点位列显示两端均未采集。
6. 在同一帧尝试同时确认两端，应明确拒绝。
7. 点击试测，应分别拟合两端目标边，状态包含起点行坐标和终点行坐标；离线图没有轴5点位时明确不输出毫米长度。两张图切换后，橙色拟合线应各自落在本图目标边上，不能串到另一端图像。
8. 保存并重新打开，确认长度模式、两端帧与ROI、两端未采集状态恢复。
9. 如现场连接硬件：必须使用相机0帧；每端确认后下方列表显示轴5编码器；两端齐全后显示`axis5_compensation(终点)-axis5_compensation(起点)`，右侧手动记录按钮不可用。轴运动、相机采集中或无有效帧时，确认应失败且旧端点保持。

本轮最小反馈：`Release|x64`结果，以及一次离线试测的完整状态文字。若有硬件，再返回最终长度和完整状态文字，用于核对公式分项。

工具栏“删除”只删除选中图形。本地图像管理位于“离线调试”菜单；正常相机流程不要求操作员导入或移除端点图。离线调试时验证两个边界：导入一张未关联图后能够从工程移除且磁盘原文件仍存在；尝试移除正在被跨图记录引用的起点或终点图时应被拒绝并显示引用记录。

### 测量配置精简与自动特征号测试

1. 首次打开时确认特征号自动显示`D1`，仍可手动输入其他编号。
2. 依次切换直径、孔径、长度、角度，确认只显示当前类型相关字段：孔径显示均布个数和测孔标定，长度显示远心标定和长度模式，角度显示角度选取/候选边对/角度结果。
3. 未勾选公差时，公称值、下偏差和上偏差应隐藏；勾选后显示并可编辑。
4. 连续新增两条长度记录且不手动改号，确认编号依次为`L1`、`L2`；手动输入`CUSTOM-1`后新增，确认该值被保留。
5. 跨图长度两端均关联后，确认列表固定显示“起点:图像N/矩形_N；终点:图像N/矩形_N”，即使两张图中的局部图形都叫`矩形_1`也能区分。

请返回：`Release|x64`结果；四种类型的字段显示结果；公差折叠结果；自动编号和手动编号结果；跨图关联完整文字；是否出现控件重叠、空白异常或切换后字段状态错误。

硬件阶段依次在长轴段起点和终点停止轴5、采集相机0帧并框选局部端部特征。返回两端原始编码器pulse、补偿位置mm、拟合线中点行坐标px、轴移动差mm、像素差px、像素修正量mm和最终长度。手工按`(终点行-起点行)×远心标定+(终点补偿位置-起点补偿位置)`复算，应与程序一致。交换起终点、缺少任一帧、轴仍在运动、相机仍在采集、任一端检测失败时都应拒绝输出。保存并重开工程后两端配置应恢复，旧计算值清空，重新采集或确认有效点位后才能重算。

正常流程不再单独点击右侧“记录点位”：确认起点帧后，下方列表应立即出现起点轴5摘要；确认终点帧后，应补齐终点轴5和补偿移动距离。右侧页应能分别查看两端完整数据，并允许只重采或清除其中一端。轴仍在运动、相机仍在采集或没有有效帧时，端点确认应失败，下方列表保持原值且不得写入0。

### 相机教学图像与配方资源测试

1. `Release|x64`编译成功后，用任一可用相机完成“开始连续采集 → 停止采集 → 载入最后一帧”。确认点击“载入最后一帧”时不再弹出PNG文件保存窗口，画布能正常显示图像。
2. 创建一个ROI并保存配方。确认配方JSON旁自动生成同名`.assets`文件夹，其中包含相机PNG；关闭软件后重新打开该配方，确认图像和ROI恢复。

本轮只需返回：编译是否成功；载入最后一帧是否仍弹出PNG保存窗口；配方旁是否生成`.assets`文件夹；重新打开配方是否正常。

没有相机硬件时，运行`tests/run_graphical_axis_panel_test.ps1`，以输出`PASS: simulated camera...`作为相机编辑器软件流程证据。`.assets`写入及重开可由模拟相机载入后的测试配方继续验证；主窗口到真实相机SDK、真实曝光和采集线程只在现场验收。

2026-09-23离线结果：`Release|x64`成功，脚本四组检查全部输出`PASS`；其中模拟相机组确认状态切换、启停命令、缓存载入、资源打包和配方重开均通过。硬件相机项保持待验。
